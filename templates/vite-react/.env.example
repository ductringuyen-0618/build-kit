# Copy to frontend/.env.local. Only VITE_-prefixed variables reach the browser.
# Leave VITE_API_BASE_URL unset in production so the app calls same-origin
# paths behind the App Platform ingress.

VITE_API_BASE_URL=http://localhost:8000
