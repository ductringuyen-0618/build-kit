# Copy to frontend/.env.local. Only VITE_-prefixed variables reach the browser,
# and they are baked into the bundle at build time.
# Backend URL for local dev. Leave unset in production so the app calls
# same-origin paths behind the App Platform ingress.
VITE_API_BASE_URL=http://localhost:8000
