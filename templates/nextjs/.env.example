# Copy to frontend/.env.local. NEXT_PUBLIC_* reaches the browser; the rest is server-only.

PORT=3000
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Server-only, when Next.js owns data access. App Platform binds ${db.DATABASE_URL}.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/app
